jQuery(function($) {

console.log('admin js');

// var $post = $('#<?php echo $div_id; ?>');
// var $select = $post.find('.limit-selector');
// var valid_fields = <?php echo json_encode($valid_fields); ?>;

// console.log('valid_fields', valid_fields);

// // add builtins
// if (valid_fields.builtin) {
//     $.each(valid_fields.builtin, function(i, field) {
//         $select.append(
//             $('<option>').val(field).text(field)
//         );
//     });
// }

// $post.on('click', '.add-limit', function(e) {
//     var $row = $post.find('.row-template').clone();
//     var field = $select.val();
//     var name = $row.find('.limit-input').attr('name');

//     $row.find('.limit-input').attr('name', name + field);
//     $row.find('label .input-prefix').text(field + ' limit');
//     $row.appendTo($post.find('.limit-rows')).show();

//     e.preventDefault();
//     e.stopPropagation();
//     return false;
// });

});
